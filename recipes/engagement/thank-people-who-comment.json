{
  "$schema": "https://crossly.net/schemas/automation-recipe-v1.json",
  "schemaVersion": 1,
  "name": "Thank people who comment",
  "description": "A comment is a public signal other buyers see. Replying keeps it at the top and makes the listing look alive, which is worth more than the reply itself.",
  "trigger": {
    "type": "listing.commented",
    "config": {}
  },
  "action": {
    "type": "inbox.send_message",
    "config": {
      "template": "Thanks for the comment! Happy to answer anything about this one."
    }
  }
}
