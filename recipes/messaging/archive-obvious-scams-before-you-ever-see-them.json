{
  "$schema": "https://crossly.net/schemas/automation-recipe-v1.json",
  "schemaVersion": 1,
  "name": "Archive obvious scams before you ever see them",
  "description": "The \"is this still available, text me at\" genre. Scored and archived rather than deleted, so a false positive is recoverable — which matters, because the score is a heuristic and a real buyer who writes tersely can trip it.",
  "trigger": {
    "type": "inbox.message_received",
    "config": {}
  },
  "action": {
    "type": "inbox.archive_scam",
    "config": {
      "scamScoreThreshold": 75
    }
  }
}
